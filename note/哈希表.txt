12. 整数转罗马数字
思路：将所有的能用于表示的罗马数字按从大到小的顺序打表（1,5开头的以及4,9开头的）。对于num,按照表从大到小判断是否能减掉当前的数，能减掉就一直减（如3000需要减3次1000）。

202. 快乐数
思路：不是快乐数的数字，计算过程中一定会成环，使用map判断是否成环（方法2：使用双指针判断链表是否成环）

128. 最长连续序列
思路：先将数组转set去重，然后只枚举连续序列中最小的数字x（set中不存在x-1的数为续序列中最小的数字）。对于x，不断地判断是否存在x+1，并更新ans